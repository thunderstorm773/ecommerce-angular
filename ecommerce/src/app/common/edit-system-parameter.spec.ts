import { EditSystemParameter } from './edit-system-parameter';

describe('EditSystemParameter', () => {
  it('should create an instance', () => {
    expect(new EditSystemParameter()).toBeTruthy();
  });
});
