import { AddProductCategory } from './add-product-category';

describe('AddProductCategory', () => {
  it('should create an instance', () => {
    expect(new AddProductCategory()).toBeTruthy();
  });
});
