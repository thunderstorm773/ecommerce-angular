import { AddSystemParameter } from './add-system-parameter';

describe('AddSystemParameter', () => {
  it('should create an instance', () => {
    expect(new AddSystemParameter()).toBeTruthy();
  });
});
