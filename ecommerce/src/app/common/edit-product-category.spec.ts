import { EditProductCategory } from './edit-product-category';

describe('EditProductCategory', () => {
  it('should create an instance', () => {
    expect(new EditProductCategory()).toBeTruthy();
  });
});
