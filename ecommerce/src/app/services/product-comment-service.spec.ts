import { ProductCommentService } from './product-comment-service';

describe('ProductCommentService', () => {
  it('should create an instance', () => {
    expect(new ProductCommentService()).toBeTruthy();
  });
});
