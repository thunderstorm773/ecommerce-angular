import { SystemParameterCodeValidator } from './system-parameter-code-validator';

describe('SystemParameterCodeValidator', () => {
  it('should create an instance', () => {
    expect(new SystemParameterCodeValidator()).toBeTruthy();
  });
});
