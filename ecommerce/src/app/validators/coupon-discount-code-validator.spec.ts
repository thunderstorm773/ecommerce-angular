import { CouponDiscountCodeValidator } from './coupon-discount-code-validator';

describe('CouponDiscountCodeValidator', () => {
  it('should create an instance', () => {
    expect(new CouponDiscountCodeValidator()).toBeTruthy();
  });
});
