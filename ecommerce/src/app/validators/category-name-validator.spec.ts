import { CategoryNameValidator } from './category-name-validator';

describe('CategoryNameValidator', () => {
  it('should create an instance', () => {
    expect(new CategoryNameValidator()).toBeTruthy();
  });
});
